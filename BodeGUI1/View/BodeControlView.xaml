<UserControl x:Class="BodeGUI1.View.BodeControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BodeGUI1.View"
             xmlns:v="clr-namespace:BodeGUI1.View"
             xmlns:vm="clr-namespace.BodeGUI1.ViewModel"
             xmlns:vmu="clr-namespace:BodeGUI1.ViewModel.UI"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="800">
    <UserControl.DataContext>
        <vmu:BodeControlsViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Content="Run" Style="{DynamicResource MaterialDesignRaisedButton}" IsEnabled="{Binding Enabled}" Command="{Binding Run}"/>
        <ContentControl Grid.Column="1" Content="{Binding Status}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vmu:StatusBase}">
                    <v:StatusView/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</UserControl>
